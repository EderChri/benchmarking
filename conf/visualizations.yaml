experiments:

  # ==========================================================================
  # Univariate (OT only): MultiViewForecaster vs RawForecaster on ETTm2
  #   Run 20  — MultiViewForecaster finetune (pretrained on ETTm1)
  #   Run 27  — RawForecaster finetune       (pretrained on ETTm1)
  #   Run 28  — RawForecaster scratch        (trained only on ETTm2)
  #   Run 29  — RawForecaster zero-shot      (pretrained on ETTm1, no ETTm2 training)
  # ==========================================================================

  # # Full test-set overview
  # - plot_type: "line"
  #   runs: [20, 27, 28, 29]

  # # Early test window — winter/spring transition
  # - plot_type: "line_weekly"
  #   runs: [20, 27, 28, 29]
  #   start_time: "2018-01-15"

  # # Late test window — different regime, checks generalization over time
  # - plot_type: "line_weekly"
  #   runs: [20, 27, 28, 29]
  #   start_time: "2018-04-09"

  # # ==========================================================================
  # # Multivariate (all 7 ETTm2 features): MultiViewForecaster vs RawForecaster
  # #   Run 22  — MultiViewForecaster finetune (pretrained on ETTm1)
  # #   Run 31  — RawForecaster finetune       (pretrained on ETTm1)
  # #   Run 32  — RawForecaster scratch        (trained only on ETTm2)
  # #   Run 33  — RawForecaster zero-shot      (pretrained on ETTm1, no ETTm2 training)
  # # ==========================================================================

  # # Full test-set overview — all 7 subplots, compare predictions per feature
  # - plot_type: "line"
  #   runs: [22, 31, 32, 33]

  # # Early test window
  # - plot_type: "line_weekly"
  #   runs: [22, 31, 32, 33]
  #   start_time: "2018-01-15"

  # # Late test window
  # - plot_type: "line_weekly"
  #   runs: [22, 31, 32, 33]
  #   start_time: "2018-04-09"
